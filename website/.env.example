# Public distribution repository (default values already work if you use these names)
GITHUB_RELEASE_OWNER=senoldogann
GITHUB_RELEASE_REPO=guardian-distribution

# Optional: token for higher GitHub API rate limits (read-only is enough)
# GITHUB_PUBLIC_READ_TOKEN=ghp_xxx
